# Clean Sweep Motion

## Commands

- `npm install`
- `npm run studio`
- `npm run test`

## Render outputs

- Transparent browser asset: `npm run render:webm`
- Quick preview still: `npm run render:still`
- Both outputs: `npm run render:all`
